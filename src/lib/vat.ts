// Flat 5% — matches the UAE FTA rate this business operates under (see
// docs/03-decisions/decision-log.md). Shared between the quote and invoice
// PDF templates so the two can't drift apart.
export const VAT_RATE = 0.05;
